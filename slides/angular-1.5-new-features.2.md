Simpler component directive
------

Define **component directives** using simple conventions:

```javascript
mod.component('myComp', {
    template: '...',
    bindings: { ... },
    controller: function() { ... }
});
```
